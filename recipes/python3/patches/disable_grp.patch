--- a/Modules/grpmodule.c
+++ b/Modules/grpmodule.c
@@ -259,18 +259,9 @@
     PyObject *d;
     struct group *p;
     if ((d = PyList_New(0)) == NULL)
         return NULL;
-    setgrent();
-    while ((p = getgrent()) != NULL) {
-        PyObject *v = mkgrent(module, p);
-        if (v == NULL || PyList_Append(d, v) != 0) {
-            Py_XDECREF(v);
-            Py_DECREF(d);
-            endgrent();
-            return NULL;
-        }
-        Py_DECREF(v);
-    }
-    endgrent();
+    /* Android NDK does not support setgrent/getgrent/endgrent.
+       Return an empty list instead. */
     return d;
 }
